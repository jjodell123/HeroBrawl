package jj;

public class DeletedStuff {
	/*
	 * This is the jump method
	 * 
	 * 
	 * public void jump() { //int direction=0; //if(jumpCount>p.getJumpLength()/2)
	 * //direction=1; /*if( !p.checkYBounds(direction)) { if(redoHolder) {
	 * System.out.println("redo"); System.out.println(p.getTheHolder()+" "
	 * +map.getHolder()); if(p.getTheHolder()<=map.getBounds().getBottom())
	 * p.setHolder(p.getTheHolder()+map.getHolder()); redoHolder=false; } jType=1;
	 * jumpCount++; p.jump(jumpCount); System.out.println(1+"j"); } else
	 * if(p.checkYBounds(direction) && jType==1 && !isGrounded()) { jType=0;
	 * map.setTileY(p.getNextHolder()-(p.getNextHolder()-p.getHolder()));
	 * if(jumpCount>p.getJumpLength()/2)
	 * p.adjust(p.getY()-map.getBounds().getTop());
	 * 
	 * //map.doAdjust(p.getY()-map.getBounds().getTop()); //
	 * System.out.println(p.getY()+" "+map.getBounds().getTop()+" hereeee");
	 * //System.out.println(p.getNextHolder()+"asd"); // map.setTileY(150); //
	 * System.out.println(direction); System.out.println(2+"j"); } else {
	 * redoHolder=true; jType=2; jumpCount++; map.jump(jumpCount); //
	 * System.out.println(3+"j");
	 * 
	 * }
	 * 
	 * 
	 * 
	 * This is the fall method
	 * 
	 * public void fall() { //type=0;
	 * 
	 * //System.out.println("fall"); //System.out.println(isGrounded());
	 * /*if(!p.checkYBounds(1)) { // System.out.println("falls"); type=1;
	 * p.fall(fallCount); System.out.println(1); } else if(type==1 &&
	 * p.checkYBounds(1)) { // System.out.println("fallen"); type=0;
	 * //map.doAdjust(p.getY()-map.getBounds().getTop());
	 * p.adjust(p.getY()-map.getBounds().getTop()); //Tile.changeAdjust();
	 * map.setTileY(p.getHolder());
	 * 
	 * System.out.println(2 + " "+p.getHolder());
	 * 
	 * } else { // System.out.println("falling"); //System.out.println("hii");
	 * //type=2; //System.out.println(fallCount); fallCount++; map.fall(fallCount);
	 * //System.out.println(3);
	 * 
	 * //p.fall(fallCount);
	 * 
	 * 
	 * if(isGrounded()) { //System.out.println("ground"); /*if(Tile.isAdjust())
	 * Tile.changeAdjust(); fall=false;s fallCount=0; /*if(type==1) {
	 * p.setY(intersected); // System.out.println(intersected+" b");
	 * 
	 * } //else if(type==2) { map.setTileY(0);
	 * map.setY((p.getY()+p.getHeight())-intersected); // p.setY(intersected); //
	 * System.out.println(intersected+" a"); } /*if(map.getBounds().getTheTop()>0) {
	 * map.doAdjust(map.getBounds().getTheTop());
	 * p.adjust(map.getBounds().getTheTop());
	 * intersected+=map.getBounds().getTheTop(); } type=0;
	 * 
	 * } //System.out.println(p.checkYBounds());
	 * 
	 * was in the headhit part of doJUmp // //Tile.changeAdjust();
	 * 
	 * // p.setY(headHit+20+p.getHeight()); //map.setY(-1*(20+p.getHeight())); //
	 * //map.setY(y); // p.setHolder(headHit+20); // Tile.changeAdjust();
	 * //sdp.adjust(p.getY()-map.getBounds().getTop()); //
	 * map.setTileY(p.getHolder()); //type=1; //map.setY(headHit-250);
	 */

}
